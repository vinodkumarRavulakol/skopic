//action imports
import * as allActions from "../../actions/actions.constants";

const initialState = {
  isLoaded: false,

  ActivityLogData: [],
};
export default function ManageContentsreducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
